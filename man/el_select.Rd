% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_select.R
\name{el_select}
\alias{el_select}
\title{Element UI Select Component}
\usage{
el_select(
  id = NULL,
  choices,
  selected = NULL,
  multiple = FALSE,
  placeholder = NULL,
  disabled = FALSE,
  clearable = FALSE,
  filterable = FALSE,
  size = NULL,
  multiple_limit = 0,
  collapse_tags = FALSE,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Input ID. Auto-generated UUID if \code{NULL}.}

\item{choices}{Named character vector (\code{c(Label = value)}) or a list of
\code{list(value = ..., label = ...)} items. Unnamed vectors are allowed; the
element is used as both value and label.}

\item{selected}{Initial selected value(s). Use a character vector for
multiple selection. Defaults to \code{""} (single) or \code{list()} (multiple).}

\item{multiple}{Whether multiple items can be selected. Default \code{FALSE}.}

\item{placeholder}{Placeholder text shown when nothing is selected.}

\item{disabled}{Whether the select is disabled. Default \code{FALSE}.}

\item{clearable}{Whether to show a clear button. Default \code{FALSE}.}

\item{filterable}{Whether typing filters the options. Default \code{FALSE}.}

\item{size}{Component size: \code{NULL}, \code{"medium"}, \code{"small"}, or \code{"mini"}.}

\item{multiple_limit}{Maximum number of items that can be selected when
\code{multiple = TRUE}. \code{0} means unlimited. Default \code{0}.}

\item{collapse_tags}{Whether to collapse selected tags into a summary when
\code{multiple = TRUE}. Default \code{FALSE}.}

\item{session}{Shiny session for module namespace support.}
}
\value{
An \code{htmltools} tagList containing the Vue-managed select component.
}
\description{
Creates an Element UI \verb{<el-select>} component backed by a Vue instance.
Supports single and multiple selection, filtering, and all standard
Element UI select props.
}
\section{Shiny input}{

\verb{input$<id>} — string (single) or character vector (multiple), updated on
each change.
}

\examples{
# Single-select from a named vector
el_select("sel1",
  choices  = c(Apple = "apple", Banana = "banana", Cherry = "cherry"),
  selected = "banana"
)

# Shiny app example
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_select("fruit",
      choices  = c(Apple = "apple", Banana = "banana", Cherry = "cherry"),
      selected = "apple",
      clearable = TRUE
    ),
    verbatimTextOutput("selected")
  )
  server <- function(input, output, session) {
    output$selected <- renderPrint(input$fruit)
  }
  shinyApp(ui, server)
}

}
