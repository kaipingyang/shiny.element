% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_pagination.R
\name{el_pagination}
\alias{el_pagination}
\title{Element UI Pagination Component}
\usage{
el_pagination(
  id = NULL,
  total,
  page_size = 10,
  current_page = 1,
  page_sizes = c(10, 20, 30, 50),
  layout = "total, sizes, prev, pager, next, jumper",
  background = FALSE,
  small = FALSE,
  disabled = FALSE,
  pager_count = 7,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Pagination ID. Auto-generated UUID if \code{NULL}.}

\item{total}{Total item count (required).}

\item{page_size}{Items per page. Default \code{10}.}

\item{current_page}{Current page number (1-based). Default \code{1}.}

\item{page_sizes}{Vector of page-size options. Default \code{c(10, 20, 30, 50)}.}

\item{layout}{Comma-separated list of layout elements.
Default \code{"total, sizes, prev, pager, next, jumper"}.}

\item{background}{Whether to use background colour on page buttons. Default \code{FALSE}.}

\item{small}{Whether to use compact (small) mode. Default \code{FALSE}.}

\item{disabled}{Whether the pagination is disabled. Default \code{FALSE}.}

\item{pager_count}{Number of pager buttons to show. Default \code{7}.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed pagination component.
}
\description{
Creates an Element UI pagination bar with page navigation, size selector,
jump-to-page input, and total count display.
}
\section{Shiny inputs}{

\describe{
\item{\verb{input$<id>_page}}{Current page number (integer).}
\item{\verb{input$<id>_size}}{Current page size (integer).}
}
}

\examples{
# Basic usage
el_pagination("pg1", total = 100)

# With custom page sizes and layout
el_pagination(
  "pg2",
  total      = 500,
  page_size  = 20,
  page_sizes = c(10, 20, 50, 100),
  layout     = "total, sizes, prev, pager, next"
)

# Shiny app example
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_pagination("pg1", total = 200),
    verbatimTextOutput("page_info")
  )
  server <- function(input, output, session) {
    output$page_info <- renderPrint({
      list(page = input$pg1_page, size = input$pg1_size)
    })
  }
  shinyApp(ui, server)
}

}
