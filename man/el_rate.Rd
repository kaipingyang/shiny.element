% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_rate.R
\name{el_rate}
\alias{el_rate}
\title{Element UI Rate (Star Rating)}
\usage{
el_rate(
  id = NULL,
  value = 0,
  max = 5L,
  disabled = FALSE,
  allow_half = FALSE,
  show_text = FALSE,
  show_score = FALSE,
  texts = c("极差", "失望", "一般", "满意", "惊喜"),
  text_color = "#1f2d3d",
  score_template = "{value}",
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Rate ID. Auto-generated UUID if \code{NULL}.}

\item{value}{Initial rating value. Default \code{0}.}

\item{max}{Maximum number of stars. Default \code{5}.}

\item{disabled}{Read-only display mode. Default \code{FALSE}.}

\item{allow_half}{Whether to allow half-star selection. Default \code{FALSE}.}

\item{show_text}{Whether to show descriptive text beside the stars. Uses the
\code{texts} vector. Default \code{FALSE}.}

\item{show_score}{Whether to show the numeric score. Default \code{FALSE}.}

\item{texts}{Character vector of length \code{max} used when \code{show_text = TRUE}.
Defaults to \code{c("极差", "失望", "一般", "满意", "惊喜")}.}

\item{text_color}{Colour of the text/score. Default \code{"#1f2d3d"}.}

\item{score_template}{Template for score display. \code{{value}} is replaced.
Default \code{"{value}"}.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed rate component.
}
\description{
A star-rating input. Supports half-stars, custom icons, and read-only display.
}
\section{Shiny input}{

\verb{input$<id>} — numeric rating value (0 to \code{max}, increments of 0.5 when
\code{allow_half = TRUE}).
}

\examples{
el_rate("rate1", value = 3)
el_rate("rate2", allow_half = TRUE, show_score = TRUE)

}
