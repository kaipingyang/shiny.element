% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_input.R
\name{el_input}
\alias{el_input}
\title{Element UI Input with Vue Instance}
\usage{
el_input(
  id = NULL,
  value = "",
  placeholder = NULL,
  type = "text",
  size = NULL,
  disabled = FALSE,
  readonly = FALSE,
  clearable = FALSE,
  show_password = FALSE,
  show_word_limit = FALSE,
  maxlength = NULL,
  rows = NULL,
  autosize = FALSE,
  prefix_icon = NULL,
  suffix_icon = NULL,
  label = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Input ID. Auto-generated UUID if \code{NULL}.}

\item{value}{Initial input value. Default \code{""}.}

\item{placeholder}{Placeholder text. \code{NULL} means no placeholder attribute.}

\item{type}{Input type: \code{"text"} (default), \code{"textarea"}, \code{"password"}.}

\item{size}{Input size: \code{NULL}, \code{"medium"}, \code{"small"}, \code{"mini"}.}

\item{disabled}{Whether the input is disabled. Default \code{FALSE}.}

\item{readonly}{Whether the input is read-only. Default \code{FALSE}.}

\item{clearable}{Whether to show a clear button. Default \code{FALSE}.}

\item{show_password}{Whether to show the password toggle icon.
Only meaningful when \code{type = "password"}. Default \code{FALSE}.}

\item{show_word_limit}{Whether to show a word-count badge.
Requires \code{maxlength} to be set. Default \code{FALSE}.}

\item{maxlength}{Maximum character count. \code{NULL} means no limit.}

\item{rows}{Number of rows for \code{type = "textarea"}. \code{NULL} uses the default.}

\item{autosize}{Whether to auto-size the textarea height. Either \code{TRUE}/\code{FALSE}
or a named list \code{list(minRows = 2, maxRows = 4)}. Default \code{FALSE}.}

\item{prefix_icon}{Icon class for the prefix slot (e.g. \code{"el-icon-search"}).
\code{NULL} means no icon.}

\item{suffix_icon}{Icon class for the suffix slot (e.g. \code{"el-icon-date"}).
\code{NULL} means no icon.}

\item{label}{ARIA \code{label} attribute for accessibility. \code{NULL} omits it.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed input component.
}
\description{
Creates an Element UI input component (\verb{<el-input>}) with a Vue instance,
supporting text, textarea, and password modes, plus clearable, prefix/suffix
icons, word-limit display, and autosize textarea.
}
\section{Shiny input}{

\verb{input$<id>} — string value of the input, updated on \code{change} event
(triggered on blur or Enter key press).
}

\examples{
# Basic text input
el_input("name", placeholder = "Enter your name")

# Clearable search input with icon
el_input("search", placeholder = "Search...",
         clearable = TRUE, prefix_icon = "el-icon-search")

# Shiny app example
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_input("txt", placeholder = "Type something"),
    verbatimTextOutput("val")
  )
  server <- function(input, output, session) {
    output$val <- renderPrint(input$txt)
  }
  shinyApp(ui, server)
}

}
