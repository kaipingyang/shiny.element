% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_date_picker.R
\name{el_date_picker}
\alias{el_date_picker}
\title{Element UI Date Picker Component}
\usage{
el_date_picker(
  id = NULL,
  value = NULL,
  type = "date",
  value_format = "yyyy-MM-dd",
  format = NULL,
  placeholder = NULL,
  start_placeholder = NULL,
  end_placeholder = NULL,
  clearable = TRUE,
  disabled = FALSE,
  editable = TRUE,
  readonly = FALSE,
  range_separator = "-",
  align = "left",
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Date picker ID. Auto-generated UUID if \code{NULL}.}

\item{value}{Initial value. A \code{Date} object, a string in the format matching
\code{value_format}, or a two-element character vector for range types. \code{NULL}
(default) leaves the picker empty.}

\item{type}{Picker type: \code{"date"} (default), \code{"datetime"}, \code{"daterange"},
\code{"datetimerange"}, \code{"month"}, \code{"year"}, \code{"week"}.}

\item{value_format}{Format string returned to Shiny when a date is selected.
Uses Element UI format tokens (e.g., \code{"yyyy-MM-dd"}). Default
\code{"yyyy-MM-dd"}.}

\item{format}{Display format shown in the input box. Uses Element UI format
tokens. \code{NULL} (default) falls back to \code{value_format}.}

\item{placeholder}{Placeholder text for non-range types.}

\item{start_placeholder}{Placeholder for the start input in range types.}

\item{end_placeholder}{Placeholder for the end input in range types.}

\item{clearable}{Whether to show the clear button. Default \code{TRUE}.}

\item{disabled}{Whether the picker is disabled. Default \code{FALSE}.}

\item{editable}{Whether the user can type directly in the input. Default \code{TRUE}.}

\item{readonly}{Whether the picker is read-only. Default \code{FALSE}.}

\item{range_separator}{Separator string displayed between start and end in
range types. Default \code{"-"}.}

\item{align}{Input alignment: \code{"left"} (default), \code{"center"}, \code{"right"}.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed date picker component.
}
\description{
Creates an Element UI date picker with Vue instance, supporting single date,
datetime, month, year, week, and date range selection modes.
}
\section{Shiny input}{

\verb{input$<id>} — String for single-date types, or two-element array for range
types. The format is controlled by \code{value_format}.
}

\examples{
# Basic date picker
el_date_picker("dp1")

# Pre-filled with today's date
el_date_picker("dp2", value = Sys.Date())

# Date range picker
el_date_picker("dp3", type = "daterange",
               start_placeholder = "Start date",
               end_placeholder   = "End date")

# Shiny app example
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_date_picker("dp1", placeholder = "Pick a date"),
    verbatimTextOutput("selected")
  )
  server <- function(input, output, session) {
    output$selected <- renderPrint(input$dp1)
  }
  shinyApp(ui, server)
}

}
