% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_checkbox_group.R
\name{el_checkbox_group}
\alias{el_checkbox_group}
\title{Element UI Checkbox Group}
\usage{
el_checkbox_group(
  id = NULL,
  choices,
  selected = NULL,
  disabled = FALSE,
  size = NULL,
  min = NULL,
  max = NULL,
  button = FALSE,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Checkbox group ID. Auto-generated UUID if \code{NULL}.}

\item{choices}{Named character vector \code{c(Label = value)} or list of
\code{list(value = ..., label = ...)} defining available options.}

\item{selected}{Character vector of initially checked values. \code{NULL} for none.}

\item{disabled}{Whether the entire group is disabled. Default \code{FALSE}.}

\item{size}{Size for button style only: \code{"medium"}, \code{"small"}, \code{"mini"}.}

\item{min}{Minimum number of checked items.}

\item{max}{Maximum number of checked items.}

\item{button}{Whether to use button-style checkboxes (\code{el-checkbox-button}).
Default \code{FALSE}.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed checkbox group component.
}
\description{
Creates an Element UI checkbox group with Vue instance, supporting individual
checkboxes or button-style variants.
}
\section{Shiny input}{

\verb{input$<id>} — character vector of currently selected values.
}

\examples{
el_checkbox_group(
  "cb1",
  choices = c("Option A" = "a", "Option B" = "b")
)

if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_checkbox_group(
      "cb1",
      choices  = c("Apple" = "apple", "Banana" = "banana"),
      selected = "apple"
    ),
    verbatimTextOutput("selected")
  )
  server <- function(input, output, session) {
    output$selected <- renderPrint(input$cb1)
  }
  shinyApp(ui, server)
}

}
