% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_progress.R
\name{el_progress}
\alias{el_progress}
\title{Element UI Progress Component}
\usage{
el_progress(
  id = NULL,
  percentage = 0,
  type = "line",
  status = NULL,
  stroke_width = 6,
  text_inside = FALSE,
  show_text = TRUE,
  color = NULL,
  width = 126,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Progress ID. Auto-generated UUID if \code{NULL}.}

\item{percentage}{Progress percentage, \code{0}–\code{100}. Default \code{0}.}

\item{type}{Progress bar type: \code{"line"}, \code{"circle"}, or \code{"dashboard"}.
Default \code{"line"}.}

\item{status}{Status theme: \code{NULL}, \code{"success"}, \code{"exception"}, or
\code{"warning"}. \code{NULL} means no status colour. Default \code{NULL}.}

\item{stroke_width}{Stroke width in pixels. Default \code{6}.}

\item{text_inside}{Whether to display the percentage text inside the bar
(only applies to \code{type = "line"}). Default \code{FALSE}.}

\item{show_text}{Whether to show the progress text. Default \code{TRUE}.}

\item{color}{Custom colour string (e.g. \code{"#409EFF"}). Overrides \code{status}
colour when set. Default \code{NULL}.}

\item{width}{Width in pixels for \code{"circle"} and \code{"dashboard"} types.
Default \code{126}.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed progress component.
}
\description{
Creates an Element UI progress bar. This is a display-only component;
update it from the server with \code{\link[=update_el_progress]{update_el_progress()}}.
}
\examples{
# Basic line progress
el_progress("prog1", percentage = 60)

# Circle progress with success status
el_progress("prog2", percentage = 100, type = "circle", status = "success")

# Dashboard style with custom colour
el_progress("prog3", percentage = 75, type = "dashboard", color = "#67C23A")

# Shiny app example
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_progress("prog1", percentage = 0),
    actionButton("go", "Advance")
  )
  server <- function(input, output, session) {
    observeEvent(input$go, {
      update_el_progress(session, "prog1", percentage = min(100, (input$go * 10)))
    })
  }
  shinyApp(ui, server)
}

}
