% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_dropdown.R
\name{el_dropdown}
\alias{el_dropdown}
\title{Element UI Dropdown Menu}
\usage{
el_dropdown(
  id = NULL,
  trigger_label = "Dropdown",
  items = list(),
  trigger = "hover",
  type = NULL,
  size = NULL,
  split_button = FALSE,
  hide_on_click = TRUE,
  placement = "bottom-end",
  disabled = FALSE,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Dropdown ID. Auto-generated UUID if \code{NULL}.}

\item{trigger_label}{Label or tag placed as the dropdown trigger. Default
\code{"Dropdown"}.}

\item{items}{A list of menu items. Each element is a named list with:
\describe{
\item{command}{Command value sent to \verb{input$<id>} on click. Required.}
\item{label}{Display text. Defaults to \code{command}.}
\item{icon}{Icon class string (e.g. \code{"el-icon-edit"}). Optional.}
\item{disabled}{Whether the item is disabled. Default \code{FALSE}.}
\item{divided}{Whether to show a divider above this item. Default
\code{FALSE}.}
}}

\item{trigger}{Trigger event: \code{"hover"} (default) or \code{"click"}.}

\item{type}{Button type when \code{split_button = TRUE}: \code{"primary"}, etc.}

\item{size}{Component size: \code{NULL}, \code{"medium"}, \code{"small"}, \code{"mini"}.}

\item{split_button}{Whether to render as a split button (main + dropdown
arrow). Default \code{FALSE}.}

\item{hide_on_click}{Whether to close the menu after an item is clicked.
Default \code{TRUE}.}

\item{placement}{Dropdown placement: \code{"bottom-end"} (default), \code{"bottom"},
\code{"bottom-start"}, \code{"top"}, \code{"top-start"}, \code{"top-end"}.}

\item{disabled}{Whether the entire dropdown is disabled. Default \code{FALSE}.}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed dropdown component.
}
\description{
A dropdown menu triggered by hover or click. Each menu item fires a
command that is reported as a Shiny input.
}
\section{Shiny inputs}{

\itemize{
\item \verb{input$<id>} — the \code{command} value of the last clicked item.
\item \verb{input$<id>_count} — click counter, incremented for each item click
(useful to detect re-clicks of the same command).
}
}

\examples{
el_dropdown("dd1", "Actions",
  items = list(
    list(command = "edit",   label = "Edit",   icon = "el-icon-edit"),
    list(command = "copy",   label = "Copy",   icon = "el-icon-document"),
    list(command = "delete", label = "Delete", icon = "el-icon-delete",
         divided = TRUE)
  )
)

}
