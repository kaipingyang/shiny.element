% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_feedback.R
\name{el_notification}
\alias{el_notification}
\title{Show Element UI Notification}
\usage{
el_notification(
  session,
  message,
  title = "",
  type = "info",
  duration = 4500,
  position = "top-right",
  show_close = TRUE,
  offset = 0
)
}
\arguments{
\item{session}{Shiny session object.}

\item{message}{Notification body text.}

\item{title}{Notification title. Default \code{""}.}

\item{type}{Notification type: \code{"info"}, \code{"success"}, \code{"warning"},
or \code{"error"}. Default \code{"info"}.}

\item{duration}{Auto-close delay in milliseconds. \code{0} disables auto-close.
Default \code{4500}.}

\item{position}{Screen corner: \code{"top-right"}, \code{"top-left"},
\code{"bottom-right"}, or \code{"bottom-left"}. Default \code{"top-right"}.}

\item{show_close}{Whether to show the close button. Default \code{TRUE}.}

\item{offset}{Distance from the corner edge in pixels. Default \code{0}.}
}
\description{
Server-side function to show a desktop-corner notification popup.
Requires \code{use_element()} or \code{el_page()} in the UI to load the JS handler.
}
\examples{
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_button("btn", "Notify", type = "primary")
  )
  server <- function(input, output, session) {
    observeEvent(input$btn, {
      el_notification(session,
        message = "Operation successful!",
        title   = "Success",
        type    = "success"
      )
    })
  }
  shinyApp(ui, server)
}

}
