% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_feedback.R
\name{el_message}
\alias{el_message}
\title{Show Element UI Message}
\usage{
el_message(
  session,
  message,
  type = "info",
  duration = 3000,
  show_close = FALSE,
  center = FALSE
)
}
\arguments{
\item{session}{Shiny session object.}

\item{message}{Message text.}

\item{type}{Message type: \code{"info"}, \code{"success"}, \code{"warning"}, or \code{"error"}.
Default \code{"info"}.}

\item{duration}{Auto-close delay in milliseconds. \code{0} disables auto-close.
Default \code{3000}.}

\item{show_close}{Whether to show the close button. Default \code{FALSE}.}

\item{center}{Whether to centre the message text. Default \code{FALSE}.}
}
\description{
Server-side function to show a top-centre message toast.
Requires \code{use_element()} or \code{el_page()} in the UI to load the JS handler.
}
\examples{
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_button("btn", "Message", type = "primary")
  )
  server <- function(input, output, session) {
    observeEvent(input$btn, {
      el_message(session,
        message = "This is a message toast.",
        type    = "warning"
      )
    })
  }
  shinyApp(ui, server)
}

}
