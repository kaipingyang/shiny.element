% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/el_slider.R
\name{el_slider}
\alias{el_slider}
\title{Element UI Slider Component}
\usage{
el_slider(
  id = NULL,
  value = 0,
  min = 0,
  max = 100,
  step = 1,
  range = FALSE,
  disabled = FALSE,
  show_input = FALSE,
  show_stops = FALSE,
  show_tooltip = TRUE,
  vertical = FALSE,
  height = NULL,
  marks = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Slider ID. Auto-generated UUID if \code{NULL}.}

\item{value}{Initial value. A single number, or \code{c(low, high)} when
\code{range = TRUE}. When \code{range = TRUE} and a scalar is supplied, the upper
bound is set to \code{max}.}

\item{min}{Minimum value. Default \code{0}.}

\item{max}{Maximum value. Default \code{100}.}

\item{step}{Step size. Default \code{1}.}

\item{range}{Whether to enable range selection. Default \code{FALSE}.}

\item{disabled}{Whether the slider is disabled. Default \code{FALSE}.}

\item{show_input}{Whether to display a numeric input box beside the slider
(non-range mode only). Default \code{FALSE}.}

\item{show_stops}{Whether to display stop markers at each step. Default \code{FALSE}.}

\item{show_tooltip}{Whether to display the tooltip when dragging. Default \code{TRUE}.}

\item{vertical}{Whether to display in vertical orientation. Default \code{FALSE}.}

\item{height}{Height of the slider in vertical mode (e.g., \code{"200px"}).
Defaults to \code{"200px"} when \code{vertical = TRUE} and not explicitly provided.}

\item{marks}{Named list of mark labels, e.g.,
\code{list("0" = "0km", "50" = "50km")}. Default \code{NULL} (no marks).}

\item{session}{Shiny session for module support.}
}
\value{
An \code{htmltools} tagList with a Vue-managed slider component.
}
\description{
Creates an Element UI slider with Vue instance, supporting single value and
range modes, marks, vertical orientation, and optional numeric input box.
}
\section{Shiny input}{

\verb{input$<id>} — Number (\code{range = FALSE}) or two-element array
(\code{range = TRUE}), updated when the user finishes dragging.
}

\examples{
# Basic usage
el_slider("slider1", value = 30, min = 0, max = 100)

# Range slider
el_slider("slider2", value = c(20, 80), range = TRUE)

# Vertical slider with marks
el_slider("slider3", value = 50, vertical = TRUE, height = "200px",
          marks = list("0" = "0km", "50" = "50km", "100" = "100km"))

# Shiny app example
if (interactive()) {
  library(shiny)
  library(shiny.element)
  ui <- el_page(
    el_slider("slider1", value = 50, min = 0, max = 100),
    verbatimTextOutput("val")
  )
  server <- function(input, output, session) {
    output$val <- renderPrint(input$slider1)
  }
  shinyApp(ui, server)
}

}
